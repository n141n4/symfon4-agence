{%extends 'base.html.twig' %}
{% block body %}
    <div class="container mt-4">
        <h3>Tous nos biens</h3>
        <div class="row">
            {% for property in properties %}
                <div class="col-md-4">
                    {% include 'property/_property.html.twig' %}
                </div>
            {% endfor %}
        </div>
        <div class="pagination mt-4">
            {{ knp_pagination_render(properties) }}
        </div>
    </div>
{% endblock %}